div.fixed-action-btn.center
    p
        i.material-icons(style="font-size : 32px;") supervisor_account
    p(style="font-size : 24px;")
        span#currentConnected=currentConnected

div.navbar-fixed
    nav
        div.nav-wrapper.light-blue
            a.brand-logo.left(href="/", style="margin-left : 8px;") El famoso
            if !user
                ul.right
                    li
                        a.white-text(href="/login") Connexion
                    li
                        a.white-text(href="/register") Inscription
            else
                ul.right
                    li.avatar
                        img.circle.responsive-img(src=user.icon, alt="",width="48px", height="48px", style="position:relative;top:7px;right:8px;")
                    li
                        a(href="/profile")
                            i.material-icons.white-text more_vert
